// (40) creo il file interface con Angular
export interface UserInterface { //(41) tipicizzo i parametri dell'array
            pnr: string;
            service: string;
            name: string;
            lastname: string;
            from: string;
            to: string;
            date: string;
}
